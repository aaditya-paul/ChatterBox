import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ChatFunction = () => {
  return (
    <View>
      <Text>ChatFunction</Text>
    </View>
  );
};

export default ChatFunction;

const styles = StyleSheet.create({});
